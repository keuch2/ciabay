*.log
.DS_Store
.env
.env.backup
.env.production
/.deploy/credentials.env
/.deploy/*.key
/.deploy/*.pem
/.deploy/known_hosts
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# /public/build is intentionally NOT ignored — the production server (Ferozo
# shared hosting) has no node/npm available, so Vite build artifacts are
# committed and shipped via git. Rebuild locally with `npm run build` before
# committing frontend changes.
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# Runtime uploads (admin-uploaded files should not be in the repo)
/storage/app/public/uploads
/storage/app/public/products
/storage/app/public/branches
/storage/app/public/brands
/storage/app/public/catalog
